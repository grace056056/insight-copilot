# Insight Copilot — Backend Dependencies
# Install: pip install -r requirements.txt

# --- Web Framework ---
fastapi==0.115.0
uvicorn==0.30.6            # ASGI server to run FastAPI
python-multipart==0.0.9    # Required for file upload handling

# --- Data Processing ---
pandas==2.2.2
numpy==1.26.4

# --- LLM Integration (Day 2+) ---
anthropic==0.34.1

# --- Utilities ---
pydantic==2.9.1             # Already a FastAPI dependency, pinned for clarity
python-dotenv==1.0.1        # Load .env for API keys
